/**
 * Selective Disclosure Wallet - Content Script
 *
 * Reserved for later: inject provider (e.g. window.selectiveDisclosure) so dApps
 * can request proofs. "Connect new wallet" is handled by the background via
 * chrome.scripting.executeScript with world: "MAIN".
 */

(function () {
  "use strict";
  console.log("[SelectiveDisclosure] Content script loaded");
})();
